[phases.setup]
nixPkgs = ["libGL", "libGLU", "python311"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip setuptools wheel", "python3 -m pip install -r requirements.txt"]

[phases.build]

[start]
cmd = "python start.py"
